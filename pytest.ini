[pytest]
addopts = -q
markers =
	slow: long-running tests (can be skipped in CI)
	network: tests that hit the network. Individual test modules gate execution behind package-specific environment variables.
