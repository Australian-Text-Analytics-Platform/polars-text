[pytest]
addopts = -q
markers =
	slow: long-running tests (can be skipped in CI)
	network: tests that hit the network (e.g. HF Hub dict downloads). Skipped by default unless LDACA_LINDERA_DICT_REPO points at a reachable HF dataset.
